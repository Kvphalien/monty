#include <monty.h>

/**
 * nop - does not do anything
 * @head : head of the stack
 * @counter : line number
 * return : no return
 * 
 * Author : Josiah Levi
 */

void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
